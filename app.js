let qNumber = 0;
let score = 0;

const quizComponent = () => {
  //call components in sequence
  //Render the Landing Page
  renderLandingPageComponent();

  renderQuestionComponent();

  //userSelectOption();
  //   renderNextQuestion;
};

$(quizComponent);
